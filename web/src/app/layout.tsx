import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  // `template` lets each page set its own title; `default` covers pages that
  // do not. Set it here once so no page ships the generator's placeholder.
  title: {
    default: "Revenue Trend Dashboard",
    template: "%s · Revenue Trend Dashboard",
  },
  description:
    "Weekly revenue, labour costs and covers for a hospitality venue, with week-on-week comparison.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
